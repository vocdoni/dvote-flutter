name: dvote_example
description: Demonstrates how to use the dvote package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  dvote:
    path: ../
